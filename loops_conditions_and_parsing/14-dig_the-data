#!/bin/bash
# Counts and groups Apache log visitors by IP and HTTP status code, then displays the occurrence number in descending order
awk '{count[$1" "$9]++} END {for (pair in count) printf "%6d %s\n", count[pair], pair}' apache-access.log | sort -nr
